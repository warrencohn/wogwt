{
    attributes = (
        {
            allowsNull = Y; 
            columnName = PHOTO; 
            externalType = object; 
            name = photo; 
            valueClassName = NSData; 
        }, 
        {
            columnName = "TALENT_ID"; 
            externalType = long; 
            name = talentID; 
            precision = 9; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (talentID); 
    className = "your.app.eo.TalentPhoto"; 
    classProperties = (photo, talent); 
    externalName = "TALENT_PHOTO"; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {
        "_clientClassPropertyNames" = (talent); 
        "_javaClientClassName" = "your.app.gwt.eo.TalentPhoto"; 
    }; 
    maxNumberOfInstancesToBatchFetch = 10; 
    name = TalentPhoto; 
    primaryKeyAttributes = (talentID); 
    relationships = (
        {
            destination = Talent; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = talentID; sourceAttribute = talentID; }); 
            name = talent; 
        }
    ); 
}
